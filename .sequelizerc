'use strict';

var Path = require('path');

var dbPath = './lib/db';

module.exports = {
    'config': Path.resolve(dbPath, 'config/config.json'),
    'migrations-path': Path.resolve(dbPath, 'migrations'),
    'models-path': Path.resolve(dbPath, 'models')
};